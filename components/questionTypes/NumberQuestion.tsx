import { QuestionProps } from "@/types/questionProps";

function NumberQuestion({ id, setData }: QuestionProps) {
    return (
        <div>
            hello
        </div>
    );
}

export default NumberQuestion;